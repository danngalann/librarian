version: '3.1'

services:
  server:
    image: server
    container_name: server
    build: ./server/
    ports:
      - "5001:80"
    volumes:
      - ./server/:/app
  client:
    image: client
    container_name: client
    build: ./client/
    ports:
      - "5002:3000"
    volumes:
      - ./client/:/app
      - /app/node_modules # Ignore node_modules